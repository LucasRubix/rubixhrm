{% extends 'base.html.twig' %}

{% block title %}Edit HandigeLinks{% endblock %}

{% block body %}
<div class="card">
    <div class="card-header maincolor whitetext">
        Edit HandigeLinks    </div>

    <div class="card-body">
    {{ include('handige_links/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('handige_links_index') }}" class="btn btn-primary bluebutton"><i class="fas fa-backward"></i></a>

    {{ include('handige_links/_delete_form.html.twig') }}

    </div>
</div>
{% endblock %}
