{% extends 'base.html.twig' %}

{% block title %}Edit Vrijvragen{% endblock %}

{% block body %}
<div class="card">
    <div class="card-header maincolor whitetext">
        Edit Vrijvragen    </div>

    <div class="card-body">
    {{ include('vrijvragen/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('vrijvragen_index') }}" class="btn btn-primary bluebutton"><i class="fas fa-backward"></i></a>

    {{ include('vrijvragen/_delete_form.html.twig') }}

    </div>
</div>
{% endblock %}
