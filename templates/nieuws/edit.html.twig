{% extends 'base.html.twig' %}

{% block title %}Edit Nieuws{% endblock %}

{% block body %}

<div class="card">
    <div class="card-header maincolor whitetext">
        Edit Nieuws
    </div>

    <div class="card-body">

    {{ include('nieuws/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('nieuws_index') }}" class="btn btn-primary bluebutton"><i class="fas fa-backward"></i></a>

    {{ include('nieuws/_delete_form.html.twig') }}

    </div>
</div>
{% endblock %}
