{% extends 'base.html.twig' %}

{% block title %}New Nieuws{% endblock %}

{% block body %}
    <div class="card">
        <div class="card-header maincolor whitetext">
            Create new Nieuws
        </div>
        <div class="card-body">
            {{ include('nieuws/_form.html.twig') }}

            <a href="{{ path('nieuws_index') }}" class="btn btn-primary bluebutton float-left">back to list</a>

        </div>

    </div>
{% endblock %}
