{% extends 'base.html.twig' %}

{% block title %}Edit Downloads{% endblock %}

{% block body %}
<div class="card">
    <div class="card-header maincolor whitetext">
        Edit Downloads    </div>

    <div class="card-body">
    {{ include('downloads/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('downloads_index') }}" class="btn btn-primary bluebutton"><i class="fas fa-backward"></i></a>

    {{ include('downloads/_delete_form.html.twig') }}

    </div>
</div>
{% endblock %}
