{% extends 'base.html.twig' %}

{% block title %}New Downloads{% endblock %}

{% block body %}
<div class="card">
    <div class="card-header maincolor whitetext">
        Create new Downloads    </div>
    <div class="card-body">
    {{ include('downloads/_form.html.twig') }}

    <a href="{{ path('downloads_index') }}" class="btn btn-primary bluebutton float-left">back to list</a>
    </div>

</div>
{% endblock %}
