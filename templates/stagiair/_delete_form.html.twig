<form method="post" action="{{ path('stagiair_delete', {'id': stagiair.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ stagiair.id) }}">
    <button class="btn btn-danger mt-5"><i class="fas fa-trash-alt"></i></button>
</form>
