{% extends 'base.html.twig' %}

{% block title %}Edit Urenverantwoording{% endblock %}

{% block body %}
<div class="card">
    <div class="card-header maincolor whitetext">
        Edit Urenverantwoording    </div>

    <div class="card-body">
    {{ include('urenverantwoording/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('urenverantwoording_index') }}" class="btn btn-primary bluebutton">back to list</a>

    {{ include('urenverantwoording/_delete_form.html.twig') }}

    </div>
</div>
{% endblock %}
