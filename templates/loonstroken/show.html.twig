{% extends 'base.html.twig' %}

{% block title %}Loonstroken{% endblock %}

{% block body %}
    <table class="table table-striped">
        <thead class="maincolor">
        <tr class="whitetext">
            <th>Loonstroken</th>
            <th></th>
        </tr>
        </thead>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ loonstroken.id }}</td>
            </tr>
            <tr>
                <th>Bestand</th>
                <td>{{ loonstroken.bestand }}</td>
            </tr>
            <tr>
                <th>Maand</th>
                <td>{{ loonstroken.maand }}</td>
            </tr>
            <tr>
                <th>Jaar</th>
                <td>{{ loonstroken.jaar }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('loonstroken_index') }}" class="btn btn-primary bluebutton">back to list</a>

    <a href="{{ path('loonstroken_edit', {'id': loonstroken.id}) }}" class="btn btn-success greenbutton float-right">edit</a>

    {{ include('loonstroken/_delete_form.html.twig') }}
{% endblock %}
