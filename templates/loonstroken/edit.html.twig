{% extends 'base.html.twig' %}

{% block title %}Edit Loonstroken{% endblock %}

{% block body %}
<div class="card">
    <div class="card-header maincolor whitetext">
        Edit Loonstroken    </div>

    <div class="card-body">
    {{ include('loonstroken/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('loonstroken_index') }}" class="btn btn-primary bluebutton"><i class="fas fa-backward"></i></a>

    {{ include('loonstroken/_delete_form.html.twig') }}

    </div>
</div>
{% endblock %}
