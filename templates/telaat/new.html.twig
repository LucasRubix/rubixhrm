{% extends 'base.html.twig' %}

{% block title %}New Telaat{% endblock %}

{% block body %}
<div class="card">
    <div class="card-header maincolor whitetext">
        Create new Telaat    </div>
    <div class="card-body">
    {{ include('telaat/_form.html.twig') }}

    <a href="{{ path('telaat_index') }}" class="btn btn-primary bluebutton float-left">back to list</a>
    </div>

</div>
{% endblock %}
