{% extends 'base.html.twig' %}

{% block title %}Edit Contract{% endblock %}

{% block body %}
<div class="card">
    <div class="card-header maincolor whitetext">
        Edit Contract    </div>

    <div class="card-body">
    {{ include('contract/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('contract_index') }}" class="btn btn-primary bluebutton">back to list</a>

    {{ include('contract/_delete_form.html.twig') }}

    </div>
</div>
{% endblock %}
