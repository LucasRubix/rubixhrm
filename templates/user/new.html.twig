{% extends 'base.html.twig' %}

{% block title %}New User{% endblock %}

{% block body %}
<div class="card">
    <div class="card-header maincolor whitetext">
        Create new User    </div>
    <div class="card-body">
    {{ include('user/_form.html.twig') }}

    <a href="{{ path('user_index') }}" class="btn btn-primary bluebutton float-left">back to list</a>
    </div>

</div>
{% endblock %}
